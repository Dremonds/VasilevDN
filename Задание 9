Ответ к заданию:

 С начала суток прошло N секунд (N — целое). Найти количество секунд, прошедших с начала последней минуты

#include <iostream>//билиотека для работы с выводом 
#include <locale.h>//библиотека для работы с языками
#include <math.h>//библиотека для работы с мат.действтями
using namespace std;//убираем команду std::

int main()
{
	int N;
	setlocale(LC_ALL, "Russian");
	cout << "С начала суток прошло N секунд(N — целое).Найти количество секунд, прошедших с начала последней минуты\nВведите N: ";
	cin >> N;
	cout << "Количество секунд: " << N % 60;//деление с остатком
	return 0;
}

 Дни недели пронумерованы следующим образом: 0 — воскресенье, 1 — понедельник, 2 — вторник, . . . , 6 — суббота. Дано целое число K, лежащее в диапазоне 1–365. Определить номер дня недели для K-го дня года, если известно, что в этом году 1 января было понедельником.


# include <iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	int K, x;
	cout << "Введите число K от 1 до 365:  ";
	cin >> K;
	x = (K % 7);//деление с остатком
	cout << "Номер дня недели = " << x << endl;
	return 0;
}
 Дни недели пронумерованы следующим образом: 1 — понедельник, 2 — вторник, . . . , 6 — суббота, 7 — воскресенье. Дано целое число K, лежащее в диапазоне 1–365, и целое число N, лежащее в диапазоне 1–7. Определить номер дня недели для K-го дня года, если известно, что в этом году 1 января было днем недели с номером N

# include <iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	int K, x, N;
	cout << "Введите число K от 1 до 365:  ";
	cin >> K;
	cout << "Введите число N от 1 до 7: ";
	cin >> N;
	//к делению с остатком прибавля ноер дня недели
	x = (K % 7)+N-1;
	//Условия необходимы для того, чтобы соблюсти правило от 1 до 7
	if (x == 8)
		x = 1;
	if (x == 9)
		x = 2;
	if (x == 10)
		x = 3;
	cout << "Номер дня недели = " << x << endl;
	return 0;
}

 Даны целые положительные числа A, B, C. На прямоугольнике размера A × B размещено максимально возможное количество квадратов со стороной C (без наложений). Найти количество квадратов, размещенных на прямоугольнике, а также площадь незанятой части прямоугольника.

# include <iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	int A, B, C, k;
	cout << "Введите A: ";
	cin >> A;
	cout << "Введите B: ";
	cin >> B;
	cout << "Введите С: ";
	cin >> C;
	//каждую сторону прямоугольника делим на сторону квадрата
	k = (A / C) * (B / C);
	cout << "Количество квадратов: " << k<<endl;
	//Из площади прямоугольника вычитаем площади квадратов
	cout << "Площадь не занятой части: " << (A * B - k * C * C);
	return 0;
}


 Дан номер некоторого года (целое положительное число). Определить соответствующий ему номер столетия, учитывая, что, к примеру, началом 20 столетия был 1901 год

# include <iostream>

using namespace std;

int main() {
	setlocale(LC_ALL, "Russian");
	int C;
	cout << "Введите год: ";
	cin >> C;
	//из года вычитаем 1 и делим на 100. После нужно прибавить 1 для точного определения века
	C = ((C-1) / 100)+1;
	cout << "Номер столетия: "<<C;
	return 0;
}

